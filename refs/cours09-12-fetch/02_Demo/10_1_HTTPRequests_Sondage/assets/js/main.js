(function () {
    const sondageListe = document.querySelector("[data-sondages]");
    const moyenne = document.querySelector("[data-sondages-moyenne]");
    const form = document.querySelector("[data-sondages-form]");

    // Récupérer des valeurs dans un formulaire

    // Préparer les données à envoyer et les convertir en JSON

    // Envoyer les données au serveur avec une requête Fetch

    // Afficher les données dans la page
})();
